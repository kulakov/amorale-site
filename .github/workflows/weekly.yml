name: weekly-update

on:
  schedule:
    - cron: '17 6 * * 1'        # каждый понедельник 06:17 UTC (~08:17 Варшава)
  workflow_dispatch:
    inputs:
      skip_scan:
        description: 'Skip the scrape, just rebuild+deploy from committed data'
        type: boolean
        default: false

permissions:
  contents: write                 # чтобы коммитить feed.xml + prev_top.json обратно

concurrency:
  group: weekly-update
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 150
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      # Проверяем токен до скана: иначе протухший токен виден только через ~40 минут,
      # в самом конце прогона. Не валим прогон — данные всё равно надо собрать и закоммитить.
      - name: Preflight — Vercel token
        continue-on-error: true
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          npx --yes vercel@latest whoami --token "$VERCEL_TOKEN" \
            || echo "::warning title=Vercel token invalid::Деплой в конце прогона упадёт. Обновить секрет VERCEL_TOKEN."

      - name: Scan Author.Today (rebuild ranking)
        if: ${{ inputs.skip_scan != true }}
        env:
          AT_LOGIN: ${{ secrets.AT_LOGIN }}
          AT_PASSWORD: ${{ secrets.AT_PASSWORD }}
          AT_TRUSTED_CODE: ${{ secrets.AT_TRUSTED_CODE }}
        run: |
          python scan.py
          test -s at_moral_final.json || (echo "scan produced no data" && exit 1)

      - name: Build site
        run: python build.py

      - name: Update RSS digest feed
        run: python feed.py

      - name: Commit snapshot + feed
        run: |
          git config user.name "amorale-bot"
          git config user.email "bot@users.noreply.github.com"
          git add feed.xml prev_top.json at_moral_final.json index.html
          git commit -m "weekly update $(date -u +%F)" || echo "nothing to commit"
          git push

      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: npx --yes vercel@latest deploy --prod --yes --token "$VERCEL_TOKEN"

# trigger registration
